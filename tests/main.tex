\documentclass[border=10mm, varwidth]{standalone}
\usepackage{minted}

\begin{document}

From the \textbf{file tree} on the left, select a different \textbf{.tex document} for another test.

\end{document}